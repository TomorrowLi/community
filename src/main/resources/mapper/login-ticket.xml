<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tomorrowli.community.community.mapper.LoginTicketMapper">
    <update id="updateStatusByUserId">
        update login_ticket set status=#{status} where ticket=#{ticket}
    </update>

    <insert id="insertTicket" parameterType="LoginTicket" keyProperty="id">
        INSERT INTO login_ticket (user_id, ticket, status, expired) VALUES (#{userId},#{ticket},#{status},#{expired})
    </insert>

    <select id="findLoginTicket" resultType="LoginTicket">
        select * from login_ticket where ticket=#{ticket}
    </select>
</mapper>
